version: 2

models:
  - name: stg_personne
    
    columns:
      - name: business_entity_id
        tests:
          - unique
          - not_null
      - name: first_name
        tests:
          - not_null
      - name: last_name
        tests:
          - not_null